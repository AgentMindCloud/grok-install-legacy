{
  "name": "grok-install",
  "version": "1.0.0",
  "description": "Mint personalized AI agents on X — open source, safety-first.",
  "main": "safety-scanner.js",
  "scripts": {
    "scan": "node safety-scanner.js",
    "test": "echo 'safety-scanner disabled (v2.14 uses schemas/v2.14/schema.json validation, run via npm run validate)'",
    "validate": "npx --yes ajv validate -s schemas/v2.14/schema.json -d 'templates/*.yaml' -d grok-install.yaml --spec=draft2020 -c ajv-formats",
    "worker:deploy": "wrangler deploy",
    "worker:dev": "wrangler dev",
    "worker:tail": "wrangler tail"
  },
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/AgentMindCloud/grok-install"
  },
  "keywords": [
    "grok",
    "x",
    "ai-agent",
    "open-source"
  ],
  "author": "@JanSol0s",
  "dependencies": {
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "wrangler": "^4.86.0"
  }
}
